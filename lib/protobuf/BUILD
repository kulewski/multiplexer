# Length-delimited protocol buffer streams, the format of the log files
# mxcontrol writes and reads; public so that other programs can read them.
cc_library(
    name = "stream",
    hdrs = ["stream.h"],
    visibility = ["//visibility:public"],
    deps = [
        "//:protobuf_runtime",
        "//lib:fd",
    ],
)
