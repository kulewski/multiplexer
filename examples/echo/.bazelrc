# The example workspace's flags: C++17, and the rules file the constants
# are generated from, so every build of this workspace uses echo.rules.
build --cxxopt=-std=c++17
# Generate the peer and message constants from this example's rules file.
build --@mx//:multiplexer_rules=//:echo.rules
